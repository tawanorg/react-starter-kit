// import { fromJS } from 'immutable';
// import { selectOrganisationDomain } from '../selectors';

describe('selectOrganisationDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
