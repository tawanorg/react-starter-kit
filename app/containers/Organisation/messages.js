/*
 * Organisation Messages
 *
 * This contains all the text for the Organisation component.
 */
import { defineMessages } from 'react-intl';

const scope = 'app.containers.Organisation';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is Organisation container !',
  },
});
